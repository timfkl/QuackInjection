{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=https://notzoom.herokuapp.com",
    "serve": "vite preview --host",
    "start": "vite preview --host"
  },
  "devDependencies": {
    
  },
  "dependencies": {
    "firebase": "^8.6.8",
    "vite": "^2.3.8"
  }
}
